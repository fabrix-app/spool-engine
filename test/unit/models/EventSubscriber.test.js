'use strict'
/* global describe, it */
const assert = require('assert')

describe('EventSubscriber Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['EventSubscriber'])
  })
})
